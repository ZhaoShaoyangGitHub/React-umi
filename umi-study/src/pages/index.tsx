import React from 'react';
import styles from './index.less';

const Home = props => {
  return (
    <div>
      <h1 className={styles.title}>Page index</h1>
    </div>
  );
};

export default Home;
